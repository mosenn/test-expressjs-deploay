<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>بازیابی رمز عبور</title>
  <link rel="stylesheet" href="/CSS/login.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
  <div class="form-container">
    <h2>ایمیل یا نام کاربری خود را وارد نمایید</h2>

    <form id="forgot-password-form" action="/admin/auth/admin-forgot-password" method="POST">
        <h2>بازیابی رمز عبور</h2>
        <p>ایمیل یا نام کاربری خود را وارد کنید تا لینک بازیابی رمز عبور برای شما ارسال شود.</p>
    
        <div class="input-group">
            <label for="emailOrUsername">ایمیل یا نام کاربری:</label>
            <input type="text" id="emailOrUsername" name="emailOrusername" required placeholder="Enter email or username">
        </div>
    
        <button type="submit" class="btn">ارسال لینک بازیابی</button>
    </form>
  </div>

  <script>
    document.getElementById("forgot-password-form").addEventListener("submit", async function(event) {
        event.preventDefault();

        const formData = new FormData(this);
        const response = await fetch(this.action, {
            method: "POST",
            body: new URLSearchParams(formData),
            headers: { "Content-Type": "application/x-www-form-urlencoded" }
        });

        const result = await response.json();

        if (response.ok) {
            Swal.fire("موفق!", result.message, "success");
        } else {
            Swal.fire("خطا!", result.message, "error");
        }
    });
  </script>
</body>
</html>
